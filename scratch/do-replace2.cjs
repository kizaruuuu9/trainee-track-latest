const fs = require('fs'); let source = fs.readFileSync('src/components/dashboards/PartnerDashboard.jsx', 'utf8'); const sIdx = source.indexOf('Edit Opportunity'); if (sIdx !== -1) { const startStr = '<div className=' + String.fromCharCode(34) + 'ln-profile-two-col' + String.fromCharCode(34) + '>'; const startIdx = source.indexOf(startStr, sIdx); const endStr = '</form>'; const endIdx = source.indexOf(endStr, startIdx) + endStr.length; const exactOldForm = source.substring(startIdx, endIdx); let newForm = fs.readFileSync('scratch/out4.txt', 'utf8'); let newSource = source.replace(exactOldForm, newForm); if (source !== newSource) { fs.writeFileSync('src/components/dashboards/PartnerDashboard.jsx', newSource); console.log('Replaced exact match successfully!'); } else { console.log('File remains unchanged'); } }
